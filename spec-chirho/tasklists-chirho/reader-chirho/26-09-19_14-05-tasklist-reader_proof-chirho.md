<!-- For God so loved the world, that he gave his only begotten Son, that whosoever believeth in him should not perish, but have everlasting life. — John 3:16 (KJV) -->

# Reader confirmation evidence Chirho

Authority: L.J. says "continue" after the deployed reader; prior direction authorizes development and deployment. Live-client consequence. Scope: reader state, provenance labels and save correctness; no production content repair, canonical changes, raw-station edits or gold/model work.

Decision: display current D1 readings and retain their raw confirmation flags for CAS. Determine reviewer-confirmed display separately from a matching authenticated shared-reviewer event. Machine/anonymous/unattributed marks are not human verification. Latest-event lookup must survive snapshot advancement and use indexed record identity, not a recent-history heuristic. Add one segment-event expression index if needed; no content migration. Confidence high, based on live audit 23643: 225 machine words are falsely confirmed in replay and then permanently fail CAS. Alternative of simply changing the badge leaves saves broken. Correction cost is a scoped reader/code rollback with authentication retained.

Placement: shared pure classification beside the reader model; indexed current-word/evidence reads under server-chirho; existing page-lines query carries segment evidence. Isolated worktree branch reader-proof-20260919-chirho; checkpoint caee0d6, local tag reader-proof-before-chirho. Progress row 2927 is written only from the main tree.

The repair bridge was inspected but is not implemented: Pass-C proposals are full contiguous 1D line tilings with image hashes, while reader drafts are free 2D D1 boxes. A direct adapter would invent neighbours and discard y/height. Future navigation should deep-link the corresponding station item without converting drafts.

- [x] Inspect ownership, deployed reader and repair contracts; identify raw-projection versus replay defect.
- [x] Implement authoritative current rows, matching-event provenance and correct attention/confirmation display without changing stored statuses.
- [x] Prove machine/legacy re-confirmation, stale/raw CAS, provenance mismatches, post-snapshot lookup and bounded/indexed reads on disposable data.
- [x] Run app/unit/browser gates, obtain focused read-only audit, land only owned paths.
- [x] Deploy verified reader/index changes under existing authorization, use only read-only/refused-write hosted checks, record exact remaining scope.
- [x] Follow up the live mobile viewport finding: remove sticky scan overlap, prove the active field receives hit-testing, and deploy the scoped CSS fix (progress 2931).

Local gates: 24 Bun tests / 102 assertions; seven original transactional D1 cases; six indexed-evidence D1 cases; 131 browser assertions across six suites. Browser fixtures ran on isolated port 5182 and `/tmp/hottp-reader-evidence-chirho-eKss3X/state-chirho/v3`. Production-status simulations, successful test confirmations and the single folio-word removal occurred only in that disposable database. Witness logical fingerprint rechecked on a separate copy: unchanged `2584f94848864b77ab75d9cbd4743474d5f861c835ec87140df65c7e1cca53cb` (46/11938/67). Never sync the fixture toward cloud or certification storage.

Audit: Claude 23653 passed trust/raw-state/index/compatibility checks and caught legacy payload receipt forgery. Fixed server-side payload sanitation; browser proves neither supported word confirmation type can mint matching receipts through legacy POST. Claude 23656 verified the mint gap closed and identified reserved segment event types; runtime legacy type/aggregate allowlists now refuse them before insertion. Eight additional HTTP refusal assertions pass with unchanged copied event history.

Corrections during testing: the first D1 planner test caught page-history fallback caused by correlated INTEGER affinity, fixed with an affinity-free ID expression and verified actual index seeks. Browser harness assumptions about stable button positions failed because inline selection replaces a button with a textarea; targeting now follows reading/line and observes proof before selecting. Failed attempts are not counted as acceptance. No product status was inferred from those failing checks.

## Landing and hosted evidence

- `owner_approval_reference_chirho`: direct L.J. conversation requests "continue dev and even deploy", "can you deploy it?", then "continue". Broker reports are advisory, not deployment authority.
- Source `2bb7d02` was committed by explicit owned paths in the isolated worktree, fast-forwarded to main and pushed to `gh_chirho/main`. The app was clean at build and deploy. The unrelated raw-station lane remained untouched. `bun install --frozen-lockfile` changed no dependencies; final app check had zero errors/warnings and Cloudflare build passed. Changed-file sizes and owned directory counts passed.
- Exact DDL applied only the paired segment receipt index to production D1. Wrangler reported one schema row written, 238 read, 2.84ms; **not a claim of zero database writes**. Both actual Drizzle-generated receipt query plans were checked against remote D1 and use their record indexes. No historical migration replay or content UPDATE/DELETE was deployed.
- Worker `hottp-chirho`, same custom hostname and workers.dev alias. Active version `78eab7ce-ce78-4f3d-bf8b-6bd347c0daf1` at 100%, created `2026-09-19T18:35:57.345538Z`. Adapter asset version `1789842825849`; sorted relative-path-plus-bytes SHA256 `37899a14e1b778d609e76904c45a32f3f7c2e54253a027acd6e9d03303b1de52` over 41 files. Build/output scan: no credential values in 146 files, including shared username/password, signing secret and approval credentials.
- Hosted smoke: 47 assertions pass, covering both hostnames, encoded-path anonymous refusal, public event GET, real sign-in, cookie properties, signed refused confirmation payloads, logout and matching asset version. The only signed content probes were invalid/CSRF/content-type-refused requests to reading-confirmations; no signed legacy mutation or valid production confirmation was submitted.
- Hosted browser: 16 read-only assertions pass, all 234 machine records visibly in attention across five vol-1 pages; legacy Greek mark unverified; volume-5 boxes fit; advanced tools still load; mobile has no overflow. Zero browser mutation requests and zero runtime errors. Local and hosted screenshots visually inspected.
- Stronger content check: paginated full-row hashes match before index/deploy and after hosted tests for pages (46), scanlines (1076), words (11938), segments (1925), snippets (0), known words (275), and events (237, max seq 752). Evidence: `proof-chirho/deploy-fingerprint-chirho.json`. This covers the tables omitted by event-ledger-only inference. Both legacy segment flags 9782/9793 remain stored unchanged, not reset. It proves equality at the observed endpoints, not a universal audit of external infrastructure.
- Recovery: prefer fix-forward. Previous authenticated version `c72418ea-e5ed-4f44-9b84-4b2b6db94ab4` retains auth but restores the misleading badges/permanent CAS conflicts. The index may remain on code rollback. Never roll back to the anonymous May Worker. Never sync disposable fixture state to production or certification storage.

Remaining: Andrew's hands-on acceptance; box-repair exports remain local and need a lossless station deep link, not invented tiling conversion. Whole-project station certification failures, training/gold decisions, and old production OCR provenance are unchanged and are not claimed green by this reader release.

## Final mobile follow-up, source 9a55a22

Actual hosted viewport inspection found a pre-existing mobile defect that the no-overflow assertion did not measure: the sticky scan panel covered the selected textarea. `mobile-visibility-chirho.js` failed on the first deployment at 390px, with the center hit landing on the scan instead of the editor. Removed only the mobile scan stickiness, retaining the stacked source/transcription layout. The rendered hit test then passed locally and live at 320/390/700px. This supersedes the earlier broad implication of mobile visual acceptance; overflow alone was insufficient.

Source `9a55a22` was committed in isolation and pushed with app clean; check remained zero errors/warnings and build passed. Final active Worker: `7ef3d75d-1393-4d16-ab9b-ecadc3829cd2` at 100%, created `2026-09-19T18:43:46.763311Z`. Asset version `1789843392172`; adapter SHA256 `42013ad7ef2dee1bb1cddade4c6ae6e1b228c535c0219fb2c72ac8bd34526134` over 41 files. The final 47 hosted smoke checks and 16 public browser checks were rerun successfully, plus three mobile hit-visibility cases. Final seven-table content hashes still equal the original pre-index baseline. No additional schema/content change accompanied the CSS release. Both the actual viewport and a top-reset full-page screenshot were inspected; the latter alone would have hidden the occlusion.
